@model IEnumerable<SrgDomain.Model.Department>
@{
    ViewData["Title"] = "Департаменти";
}
<h2>Департаменти</h2>
<p>
    <a asp-action="Create">Створити новий департамент</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.First().DepartmentName)</th>
            <th>@Html.DisplayNameFor(model => model.First().Description)</th>
            <th>@Html.DisplayNameFor(model => model.First().ContactEmail)</th>
            <th>@Html.DisplayNameFor(model => model.First().CreatedDate)</th>
            <th>Менеджер</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.DepartmentName)</td>
                <td>@Html.DisplayFor(modelItem => item.Description)</td>
                <td>@Html.DisplayFor(modelItem => item.ContactEmail)</td>
                <td>@item.CreatedDate.ToString("g")</td>
                <td>
                    @if (item.Manager != null)
                    {
                        <a asp-controller="Managers" asp-action="Details" asp-route-id="@item.Manager.Id">Подивитися менеджера</a>
                    }
                    else
                    {
                        <a asp-controller="Managers" asp-action="Create" asp-route-departmentId="@item.Id">Додати менеджера</a>
                    }
                </td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.Id">Дані</a> |
                    <a asp-action="Edit" asp-route-id="@item.Id">Редагувати</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Видалити</a>
                </td>
            </tr>
        }
    </tbody>
</table>
